<% provide(:title, @user.name) %>
<div class="container pt-4">
    <div class="row">
        <div class="col-3">
            <section>
                <h1>
                <%= @user.name %><br>
                <%= link_to user_avatar(current_user, size: 35), current_user %>
                </h1>
            </section>
        </div>
        <div class="col-9">
        <p>feed</p>
        </div>
    </div>
</div>